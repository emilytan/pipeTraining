import { TestIfDirective } from './test-if.directive';

describe('TestIfDirective', () => {
  it('should create an instance', () => {
    const directive = new TestIfDirective();
    expect(directive).toBeTruthy();
  });
});
