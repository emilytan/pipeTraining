import { ObjectNgForDirective } from './object-ng-for.directive';

describe('ObjectNgForDirective', () => {
  it('should create an instance', () => {
    const directive = new ObjectNgForDirective();
    expect(directive).toBeTruthy();
  });
});
